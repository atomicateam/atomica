sudo: required
dist: xenial
python: "3.7"
language: python
install: pip install tox-travis
before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start
script: tox
