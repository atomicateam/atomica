# Python package
# Create and test a Python package on multiple Python versions.
# Add steps that analyze code, save the dist with the build record, publish to a PyPI-compatible index, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/python

variables:
PYTEST_ADDOPTS: "--junitxml=build/test-results/$(TOXENV).xml"

jobs:
- job: 'tox'

  pool:
    vmImage: 'ubuntu-16.04'
  strategy:
    matrix:
      Python36:
        python.version: '3.6'
        TOXENV: 'py36'
      Python37:
        python.version: '3.7'
        TOXENV: 'py37'

  steps:
  - task: UsePythonVersion@0
    displayName: 'Use Python $(python.version)'
    inputs:
      versionSpec: '$(python.version)'

  - script: python -m pip install --upgrade pip && python -m pip install tox

    displayName: 'Install Tox'

  - script: tox -e $(TOXENV)
    displayName: 'Run Tox'

  - task: PublishTestResults@2
    inputs:
      testResultsFiles: 'build/test-results/$(TOXENV).xml'
      testRunTitle: '$(tox.env)'
    condition: succeededOrFailed()



#- task: TwineAuthenticate@0
#  inputs:
#    artifactFeeds: 'feed_name1, feed_name2'
#    externalFeeds: 'feed_name1, feed_name2'
#- script: 'twine -r {feedName/EndpointName} --config-file $(PYPIRC_PATH) {package path to publish}'